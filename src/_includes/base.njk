<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>
    {{ title + ' | ' + site.title if title and title != 'White Mountain Jackass Company' else site.title }}
  </title>
  <meta name="description" content="{{ site.meta_desc }}">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  {# <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-LN+7fdVzj6u52u30Kp6M/trliBMCMKTyK833zpbD+pXdCLuTusPj697FH4R/5mcr" crossorigin="anonymous"> #}
  <link rel="stylesheet" href="{{ '/css/styles.css' | url }}" />
</head>
<body>

  <div class="container">
    <header id="site-header" role="banner">
      {% include "nav.njk" %}
    </header>

    <main id="site-main" role="main">
      {% if page.url != '/' %}
        <div class="link-back">
          <a href="/">
            <img src="/images/logo-sm.gif" width="300" height="170" alt="White Mountain Jackass Company" class="img-responsive">
          </a>
        </div>
      {% endif %}
      {{ content | safe }}
    </main>

    <footer id="site-footer" role="contentinfo"{% if page.url === '/' %} style="margin:50px 0 0;"{% endif %}>
      {% include "footer.njk" %}
    </footer>
  </div>

</body>
</html>